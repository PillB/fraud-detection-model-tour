# Core dependencies for reproducible fraud detection experiments and pipeline
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
networkx>=3.0
imbalanced-learn>=0.11

# Testing
pytest>=7.4
pytest-cov>=4.1

# Optional for advanced GNN experiments (uncomment if needed)
# torch>=2.0
# torch-geometric>=2.4   # Requires compatible torch; heavy. Use for full TGN experiments.

# For website build / serve (if using any tooling)
# No heavy frontend deps — self-contained Tailwind CDN used in website/
